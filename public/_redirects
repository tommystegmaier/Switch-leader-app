# SPA fallback for client-side routing (Cloudflare Pages / Netlify).
# Every path that isn't a real static file serves index.html, so per-workspace
# deep links like /o/your-app work on refresh and direct open.
/*    /index.html   200
